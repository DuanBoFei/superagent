# 会话交接 · web-server-start

## 上次做到哪
已完成 T001 和 T002：`src/cli/web.ts` 命令入口骨架已创建，顶层 CLI 模式解析已支持 `superagent web --port --verbose --no-open` 并调用 `startWebCommand()`。

## 下次会话要做的事
1. 先读宪法（constitution.md）
2. 读 state.md → 当前任务是 T003
3. 从 T003 继续，禁止重新规划

## 禁止重新规划
plan.md 已经定稿，tasks.md 已经锁定。
直接执行，不要再 re-plan。
