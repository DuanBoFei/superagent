# 会话交接 · web-server-start

## 上次做到哪
已完成 T001：创建 `src/cli/web.ts` 命令入口骨架，并补充 `tests/cli/web.test.ts`。

## 下次会话要做的事
1. 先读宪法（constitution.md）
2. 读 state.md → 当前任务是 T002
3. 从 T002 继续，禁止重新规划

## 禁止重新规划
plan.md 已经定稿，tasks.md 已经锁定。
直接执行，不要再 re-plan。
